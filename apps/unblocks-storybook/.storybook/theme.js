import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: '📘 Unblocks Stories',
  brandUrl: 'https://github.com/kristw/unblocks',
  brandTarget: '_self',
});
